// 1、输出结果为什么是3 3 3
for(var i=0;i<3;++i){
	setTimeout(function(){
		console.log(i);
	},0);
}

// 2、使用js实现一个promise，要求支持resolve、reject、done、fail、then、always


// 3、两个顺序排列的数组A和B，求B数组是否为A数组的子集（数组内可能有重复数组）

// 4、写出三个不同的this变量使用场景